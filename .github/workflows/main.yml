name: Java CI with Maven
on:
  push:
    branches: [master]
jobs:
  build:
    runs-on: ubuntu-latest
    environment: Prod
    env:
      DEPLOY_MAWE: ${{ vars.DEPLOY_NAME }}
    steps:
      - name: Print variable
        run: echo "Deploying to $DEPLOY_NAME" 
